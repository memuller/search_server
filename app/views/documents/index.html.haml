- if @site
  = render "site_info"
- else
  %h1= 'Listing all documents'

.search
  = form_tag documents_path, method: 'get', id: 'documents_search' do
    = text_field_tag :string, params[:string]
    = submit_tag  "Search"


= link_to 'New Document', new_document_path
  
= render 'results'